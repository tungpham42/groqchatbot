[build]
  command = "npm run build"
  publish = "build"

[functions]
  node_bundler = "esbuild" # Giúp bundle code nhanh hơn

[functions."keep-alive"]
  schedule = "0 12 * * *"
  
# Redirect để gọi API thân thiện hơn
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200